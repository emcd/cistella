## 1. Verb slate and profile schema

- [ ] 1.1 Rename CLI verbs to all-Latinate: `run`->`conduct`, `status`->`survey`, `logs`->`inspect`, `doctor`->`check`, `stop`->`terminate`, `exec`->`enter`, `gc` kept with no alias; remove harness flag, add `--directory` (optional, defaults to canonical cwd, no `--cwd` alias) and `--identity` (label, not credential selector) and `--label k=v` (refuse `cistella.` prefix), `command` after `--` (TOML array, never shell string); update `src/cli.rs` and `src/main.rs` help
- [ ] 1.2 Move `image` (tag or digest, required, resolved digest in `cistella.image`) and optional `command` (array) into profile TOML (`data/profiles/<name>.toml` resolved by name/path), with `container_home` (optional default) and `env` (optional) and `labels` table; retire synthetic TOML-by-interpolation in `src/main.rs:137`; update `Profile` struct and validation (harness removed)
- [ ] 1.3 Update labels: `cistella.id` (minted lowercase `[a-z0-9]` fixed length, time-sortable, >=40 bits), `cistella.directory` (canonical), `cistella.profile`, `cistella.profile-digest`, `cistella.identity` (was seat), `cistella.command` (argv array), `cistella.image` (resolved digest); unit name `cistella-<id>.container` (no harness), `Tmpfs=<canonical container_home>`

## 2. Conduct owns lifecycle

- [ ] 2.1 Implement `conduct` mint (lowercase Crockford base32 ms+40 random bits, `[a-z0-9]` fixed length, time-sortable, >=40 bits), `flock $XDG_RUNTIME_DIR/cistella/lock` acquired before any unit-file or scratch creation and held through `create unit -> start` -> `podman exec -i -t` harness argv after `--` (or profile `command` array) on pane PTY -> wait -> shared teardown; traps `SIGHUP`/`SIGTERM`, `0x03` in-band; exit is harness status or `128+signal` after `SIGHUP`/`SIGTERM` (S5), teardown on every failure path past install (S3), and test `gc` blocked at each creation phase (M3)
- [ ] 2.2 Unify teardown: `teardown(id)` = `flock` lock -> stop unit (fail closed unless `LoadState=not-found` via `show`) -> wait `ActiveState` inactive/failed -> remove `.container` (session id resolved via `Label=` before removal) -> `daemon-reload` -> `reset-failed` -> remove `XDG_RUNTIME_DIR/cistella/<id>` scratch (fallback `/tmp`); `terminate` (any-state, via `show`) and `gc` both call it, no `disable`, no `rfind`, no substring prose; add `gc` creation-window scenario "gc during conduct creation reaps nothing" (M3, `flock`)
- [ ] 2.3 Update `survey` (zero or many selectors as filters) and `enter`/`inspect`/`terminate` (exactly one selector per invocation — `<id>` prefix or `--directory` or `--label`, exclusive, `--directory` canonicalized, `--label` split on first `=`, I2/I4, ambiguous typed refusal) to join registry `cistella-*.container` + `systemctl show ActiveState` + `podman ps` labels; add `cistella.command` JSON array round-trip test (`argv -> label -> argv` equals input); `conduct`/`enter` forward closed `TERM` at exec time (S6)

## 3. Mount and identity updates

- [ ] 3.1 Harden `container_home` canonicalization (`..` traversal, sensitive roots) and injection guards (`=`/`\n`/`\0`, env key `[A-Z_][A-Z0-9_]*`, volume `=` rejection, session field `[A-Za-z0-9._-]` etc) for all interpolated Quadlet values; `Tmpfs=<canonical container_home>` (M4), `HOME` scenario vs `container_home` (M5)
- [ ] 3.2 Keep per-harness shared state RW, notebook RW/RO, two-tier topology (session-home first, triples by depth); `directory` mount at `/work`, scratch is `XDG_RUNTIME_DIR/cistella/<id>` (per-user tmpfs, same lifetime as user manager; fallback `/tmp`, S10)

## 4. Validation

- [ ] 4.1 Update `tests/integration/cli_lifecycle.rs` to `conduct`/`enter`/`survey`/`inspect`/`terminate`/`gc` (no `clear` alias): minted id matches `[a-z0-9]` fixed length/time-sortable and caller-supplied id rejected; lifecycle `conduct` -> `enter` (`stty size` 30 100 via `cistella enter` under PTY) -> `survey` (zero/many filters) -> `terminate` (exactly one selector, from another pane while `conduct` attached, both converge, not in `--failed` S4) -> `inspect` (exactly one selector, `journalctl` post-mortem, typed error if journal unavailable S8) -> `gc`; orphan `systemctl stop` -> `gc` reaps unit+scratch without manual pre-cleanup (sessions addressed by `enter --directory` selector, never by caller-supplied id, `conduct` failure after install leaves no residue S3, pane `kill-pane` S5, selector ambiguous/exclusive S7, `cistella.command` JSON round-trip)
- [ ] 4.2 Run `cargo clippy -- -D warnings` and `cargo nextest` green with `TERM=xterm-ghostty` on `debian:bookworm-slim` baked `xterm-ghostty`
